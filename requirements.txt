# Core dependencies. Install with:  pip install -r requirements.txt
# PyTorch: for a CUDA build, follow https://pytorch.org/get-started/locally/

torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
PyYAML>=6.0
matplotlib>=3.7
shap>=0.44          # only needed for --shap
tqdm>=4.65

# Optional, for tests
pytest>=7.4

# Optional: the fused CUDA kernel for the Mamba model. The repository ships a
# portable PyTorch fallback, so this is a speed-up, not a requirement.
# mamba-ssm>=2.0
